{
  "name": "angular-demo",
  "version": "1.0.0",
  "description": "xiedan angular-demo",
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "./node_modules/.bin/hs -a localhost -p 9000 -o",
    "pretest":"npm install",
    "test":"./node_modules/.bin/browser-sync start --server app --files 'app/index.html' --no-notify"
  },
  "keywords": [
    "xiedan",
    "hope-xd",
    "angular",
    "demo"
  ],
  "author": "xiedan (http://www.xiedan.club/)",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "browser-sync": "^2.11.1",
    "http-server": "^0.9.0"
  }
}
